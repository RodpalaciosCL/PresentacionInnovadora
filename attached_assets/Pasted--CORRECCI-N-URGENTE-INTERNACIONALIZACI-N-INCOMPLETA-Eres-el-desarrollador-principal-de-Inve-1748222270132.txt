# 🚨 CORRECCIÓN URGENTE — INTERNACIONALIZACIÓN INCOMPLETA

Eres el desarrollador principal de **Invenor** en Replit. El selector de idioma **ES/EN no está funcionando correctamente**: muchas secciones siguen sin traducir, otras muestran claves rotas (`about.hero.title`) o textos hardcoded. Este post es para que hagas **una corrección total, en bloque único**.

---

## 📸 EVIDENCIA (revisar imágenes subidas)

1. Se muestra `about.hero.title`, `about.approach.items.undefined.title`, etc.
2. Traducciones inexistentes o `undefined` en claves dinámicas.
3. Secciones **enteras** como Projections, Contact, Opportunities siguen en español.
4. Placeholder, textos de inputs, gráficos, botones, títulos y subtítulos sin traducir.
5. Footer sin año dinámico ni texto traducido.

---

## ✅ INSTRUCCIONES – BLOQUE ÚNICO DE CORRECCIÓN

### 1. Validar cada `t('…')`
- Recorre **TODO el código fuente** (`pages`, `components`, `layouts`, `drawers`, etc.).
- Por cada `t('clave')`, asegúrate de que **esa clave exista en ambos archivos**:
  - `src/locales/es.json`
  - `src/locales/en.json`
- Si no existe, créala.
  - En español: texto original correcto.
  - En inglés: traducción real (no `"TODO"`).

### 2. Arreglar claves rotas como `about.approach.items.undefined.title`
- Usa claves explícitas, **no interpolación con IDs** si el objeto puede estar vacío:
  - Cambia:
    ```tsx
    t(`about.approach.items.${item.id}.title`)
    ```
  - Por algo más seguro:
    ```tsx
    t(item?.titleKey ?? 'about.approach.items.fallback.title')
    ```

### 3. Reemplazar 100% de los textos visibles
- Sustituye cada texto hardcoded del sitio, incluyendo:
  - `<h1>`, `<h2>`, `<p>`, subtítulos, cards
  - Tooltips, tags, modales
  - Placeholders de formularios
  - Labels y mensajes de validación
  - Botones como “Enviar”, “Ver más”, etc.
  - Descripciones de gráficos o tablas
  - Títulos en leyendas o pestañas
- Todos deben pasar a estar bajo `t()`.

### 4. Footer dinámico y traducido
- Cambia línea final del footer:
  ```tsx
  © {new Date().getFullYear()} Invenor. {t('footer.rights')}
Añade en archivos:

es.json → "footer.rights": "Todos los derechos reservados."

en.json → "footer.rights": "All rights reserved."

5. Revisión visual total
Ejecuta la app.

En cada ruta del sitio (/, /about, /opportunities, /projections, /contact) cambia entre ES/EN:

Verifica que no quede ninguna palabra sin traducir.

Verifica que ningún t('…') se muestre como literal.

Que no se muestre "undefined", ni "TODO", ni claves huérfanas.

Si alguna clave no cambia, asume que no está registrada o mal llamada.

🟢 AL TERMINAR RESPONDE:
“🟢 Internacionalización corregida. Todo el sitio funciona 100% en ES y EN. Sin claves faltantes, sin textos hardcoded ni rotos.”

Este cambio es fundamental para el uso real de la web con inversionistas internacionales.
Hazlo todo en una sola pasada, y con extrema rigurosidad.